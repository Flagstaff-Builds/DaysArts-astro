import { createApp } from 'vue'

// This file serves as a global entry point for Vue configuration
export default function createVueApp() {
  const app = createApp({})
  
  // Add any global Vue configurations here
  
  return app
}
